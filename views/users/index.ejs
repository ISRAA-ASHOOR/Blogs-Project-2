<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <title>Users</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="index">All IBlog Users</h1>
    <ul>
        <% users.forEach(user => { %>
            <div class="cards">
                <a href="/users/<%= user._id %>">
                <li><%= user.username %></li>
                </a>
            </div>
        <% }); %>
    </ul>
</body>
</html>