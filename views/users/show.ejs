<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <title><%= users.username %> 's Blogs</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="index"><%= users.username %> 's blogs</h1>
        <% const blogs = users.blogs %>
        <div class="blog-cards">
        <% blogs.forEach((blog) => { %>
            <a href="/users/<%= users._id %>/blog/<%= blog._id %>">
                <p><img src="<%= blog.image %>" class="blog-img"/> <br /> <strong><%= blog.title %></strong></p>
            </a>
        <% }) %>
        </div>
</body>
</html>